package server

import (
	"testing"
)

func TestDelegateLiveAbort(t *testing.T) {
	// Need to setup minimal server environment.
	// Assuming s := newMockServer(t) is available in a _test.go file in same pkg.
	t.Skip("Need full mock server setup")
}
